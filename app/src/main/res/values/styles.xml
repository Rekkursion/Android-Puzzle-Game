<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.NoActionBar">
        <!--<item name="colorPrimary">@color/colorPrimary</item>-->
        <!--<item name="colorPrimaryDark">@color/colorPrimaryDark</item>-->
        <!--<item name="colorAccent">@color/colorAccent</item>-->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/drawable_window_background</item>
        <!-- for using transition animations -->
        <item name="android:windowContentTransitions">true</item>
    </style>

    <!-- difficulty seek-bar style -->
    <style name="styleDifficultySeekBar" >
        <item name="android:maxHeight">12dp</item>
        <item name="android:minHeight">12dp</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">@color/colorAccent</item>
        <item name="android:progressDrawable">@drawable/drawable_difficulty_seek_bar</item>
        <item name="android:thumb">@drawable/ic_square_orange_24dp</item>
    </style>

</resources>
